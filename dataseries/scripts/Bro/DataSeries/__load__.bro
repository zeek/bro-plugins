#
# This is loaded when a user @loads the plugin. Include scripts here that
# should be loaded automatically at that point.
#
# (For scripts that should always be loaded at startup, even without the user
# explicitly using a @load for the plugin, use the top-level
# scripts/__load__.bro instead.)

@load ./main.bro

